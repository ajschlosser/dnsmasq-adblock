# Host interface address where Docker should expose DNS.
#
# Use the stable/reserved IP address of the machine running this container.
# Example:
#
#   DNS_BIND_IP=10.0.0.98
#
# Avoid 0.0.0.0 unless you intentionally want this DNS server exposed on every
# host interface.
DNS_BIND_IP=10.0.0.98